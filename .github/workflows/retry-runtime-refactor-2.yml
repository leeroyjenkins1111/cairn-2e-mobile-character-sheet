name: Retry runtime refactor 2

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  refactor:
    if: github.head_ref == 'refactor/runtime-registry-and-entrypoint' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Execute corrected migration
        run: |
          python3 <<'PY'
          from pathlib import Path
          import textwrap
          workflow = Path('.github/workflows/apply-runtime-refactor.yml').read_text(encoding='utf-8')
          workflow = workflow.replace("('drawPhysicalFaceValue', 'context, entry, label, reveal')", "('drawPhysicalFaceValue', 'context, face, label, reveal, isLight')")
          script = workflow.split("python3 <<'PY'\n", 1)[1].split('\n          PY', 1)[0]
          exec(textwrap.dedent(script), {'__name__': '__main__'})
          check = Path('scripts/check-production-runtime.mjs')
          text = check.read_text(encoding='utf-8')
          text = text.replace("productionSource.includes('document.createElement('style')')", "productionSource.includes(\"document.createElement('style')\")")
          check.write_text(text, encoding='utf-8')
          Path('.github/workflows/retry-runtime-refactor-2.yml').unlink()
          PY
      - name: Validate
        run: |
          npm run check:syntax
          npm run test:unit
          npm run check:version
          npm run check:production
      - name: Commit refactor
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Skonsoliduj rejestry runtime i punkt wejścia"
          git push origin HEAD:${{ github.head_ref }}
